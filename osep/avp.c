#include <stdio.h>
#include <stdlib.h>
#include <gtk.h>
#include <stdbool.h>

int main(verif == 0){
    int avsf; 
    int tof;
    int str[100];
    int strn;
    while (avtest == 1){
        FILE* file = fopen(filePath, "rb");
        fscan("%d", tof);
        str[strn - 1] = tof;
        strn = strn - 1;
        if (str[] == 00000){
            avsf = 0;
            printf("Antivirus Plus Defended You! -- suspicious.zero.multi");
        } else {avsf = 1;}
        if (eof == 1) {
            avtest == 0;
        }
    }
    return 0;
}
#include <stdio.h>
#include <stdbool.h>
#include <string.h>
int main(){
    int character[100];
    printf("--> Booting log.sys <--");
    FILE *fopen( const char * restrict log.sys, const char * restrict r+ )
    fprintf("bUp?/s=y.;");
    printf("--> Booting elin.sys <--");
    int fclose(log.sys *fp);
    FILE *fopen( const char * restrict elin.sys, const char * restrict r );
    fread("%d" &character);
}